<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codephrase.app">

    <application
        android:name=".SimpleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SimpleApp">

        <activity
            android:name=".activity.LauncherActivity"
            android:noHistory="true"
            android:theme="@style/Theme.SimpleApp.Launcher">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="simple-app" />
            </intent-filter>

        </activity>

        <activity android:name=".activity.FullscreenPageActivity" />

        <activity android:name=".activity.ToolbarPageActivity" />

        <activity android:name=".activity.CollapsingPageActivity" />

        <activity android:name=".activity.FragmentPageActivity" />

        <activity android:name=".activity.DataBindingActivity" />

        <activity android:name=".activity.ToolbarDrawerActivity" />

    </application>

</manifest>